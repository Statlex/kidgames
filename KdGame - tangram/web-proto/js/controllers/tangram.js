(function (win) {

	"use strict";
	/*global window, document */

	var tangram;
	tangram = {
		handleEvent: function() {

		},
		init: function() {

			var wrapper = $('#wrapper');

			console.log(wrapper);

		}


	};

	win.tangram = tangram;

	//win.addEventListener('load', tangram, false);

}(window));
